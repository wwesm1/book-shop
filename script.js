let CATALOGUE = [
  {
    id: 1,
    title: "The Name of the Wind",
    author: "Patrick Rothfuss",
    genre: "fantasy",
    language: "english",
    price: 24.99,
    rating: 4.8,
    image: "https://covers.openlibrary.org/b/id/8323594-L.jpg"
  },
  {
    id: 2,
    title: "Dune",
    author: "Frank Herbert",
    genre: "fantasy",
    language: "english",
    price: 27.99,
    rating: 4.9,
    image: "https://covers.openlibrary.org/b/isbn/9780441013593-L.jpg"
  },
  {
    id: 3,
    title: "Harry Potter and the Prisoner of Azkaban",
    author: "J.K.Rowling",
    genre: "fantasy",
    language: "english",
    price: 19.99,
    rating: 4.9,
    image: "https://d3ddkgxe55ca6c.cloudfront.net/assets/t1711556505/a/f1/a0/158647-ml-2252033.jpg"
  },
  {
    id: 4,
    title: "The Master and Margarita",
    author: "Mikhail Bulgakov",
    genre: "fantasy",
    language: "russian",
    price: 18.99,
    rating: 4.9,
    image: "https://covers.openlibrary.org/b/isbn/9780679760801-L.jpg"
  },
  {
    id: 5,
    title: "A Brief History of Time",
    author: "Stephen Hawking",
    genre: "science",
    language: "english",
    price: 18.99,
    rating: 4.7,
    image: "https://covers.openlibrary.org/b/isbn/9780553380163-L.jpg"
  },
  {
    id: 6,
    title: "Sapiens",
    author: "Yuval Noah Harari",
    genre: "science",
    language: "english",
    price: 22.99,
    rating: 4.6,
    image: "https://covers.openlibrary.org/b/isbn/9780062316097-L.jpg"
  },
  {
    id: 7,
    title: "1984",
    author: "George Orwell",
    genre: "science",
    language: "english",
    price: 15.99,
    rating: 4.8,
    image: "https://covers.openlibrary.org/b/isbn/9780451524935-L.jpg"
  },
  {
    id: 8,
    title: "Cosmos",
    author: "Carl Sagan",
    genre: "science",
    language: "english",
    price: 26.99,
    rating: 4.9,
    image: "https://covers.openlibrary.org/b/isbn/9780345539434-L.jpg"
  },
  {
    id: 9,
    title: "Pride and Prejudice",
    author: "Jane Austen",
    genre: "romance",
    language: "english",
    price: 12.99,
    rating: 4.7,
    image: "https://covers.openlibrary.org/b/isbn/9780141439518-L.jpg"
  },
  {
    id: 10,
    title: "If Love Had a Price",
    author: "Ana Huang",
    genre: "romance",
    language: "english",
    price: 20.99,
    rating: 4.4,
    image: "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTke7xOKWVjJAcQqt-POB3Q14qVFF-aZ9Xbcw&s"
  },
  {
    id: 11,
    title: "Crime and Punishment",
    author: "Fyodor Dostoevsky",
    genre: "romance",
    language: "russian",
    price: 17.99,
    rating: 4.8,
    image: "https://covers.openlibrary.org/b/isbn/9780486415871-L.jpg"
  },
  {
    id: 12,
    title: "War and Peace",
    author: "Leo Tolstoy",
    genre: "romance",
    language: "russian",
    price: 21.99,
    rating: 4.7,
    image: "https://covers.openlibrary.org/b/isbn/9780199232765-L.jpg"
  },
  {
    id: 13,
    title: "O'tgan Kunlar",
    author: "Abdulla Qodiriy",
    genre: "romance",
    language: "uzbek",
    price: 9.99,
    rating: 4.8,
    image: "https://backend.book.uz/user-api/img/img-file-5ae6f33d4f57dd353bcc444c84bb326f.jpg"
  },
  {
    id: 14,
    title: "Yulduzli Tunlar",
    author: "Pirimqul Qodirov",
    genre: "romance",
    language: "uzbek",
    price: 11.99,
    rating: 4.5,
    image: "https://backend.book.uz/user-api/img/img-27f555039d38a8802959416f8ef2b5f3.jpg"
  },
  {
    id: 15,
    title: "Kecha va Kunduz",
    author: "Cho'lpon",
    genre: "romance",
    language: "uzbek",
    price: 8.99,
    rating: 4.6,
    image: "https://backend.book.uz/user-api/img/img-file-c8e3b040b1aa5fdf23005f42889395b9.jpg"
  },
  {
    id: 16,
    title: "Clean Code",
    author: "Robert C. Martin",
    genre: "programming",
    language: "english",
    price: 35.99,
    rating: 4.6,
    image: "https://covers.openlibrary.org/b/isbn/9780132350884-L.jpg"
  },
  {
    id: 17,
    title: "The Pragmatic Programmer",
    author: "David Thomas & Andrew Hunt",
    genre: "programming",
    language: "english",
    price: 39.99,
    rating: 4.8,
    image: "https://covers.openlibrary.org/b/isbn/9780135957059-L.jpg"
  },
  {
    id: 18,
    title: "Introduction to Algorithms",
    author: "Cormen, Leiserson & Rivest",
    genre: "programming",
    language: "english",
    price: 64.99,
    rating: 4.5,
    image: "https://covers.openlibrary.org/b/isbn/9780262033848-L.jpg"
  },
  {
    id: 19,
    title: "Thinking, Fast and Slow",
    author: "Daniel Kahneman",
    genre: "psychology",
    language: "english",
    price: 19.99,
    rating: 4.7,
    image: "https://covers.openlibrary.org/b/isbn/9780374533557-L.jpg"
  },
  {
    id: 20,
    title: "Man's Search for Meaning",
    author: "Viktor E. Frankl",
    genre: "psychology",
    language: "english",
    price: 14.99,
    rating: 4.9,
    image: "https://covers.openlibrary.org/b/isbn/9780807014295-L.jpg"
  },
  {
    id: 21,
    title: "12 Rules for Life",
    author: "Jordan B. Peterson",
    genre: "psychology",
    language: "english",
    price: 23.99,
    rating: 4.4,
    image: "https://covers.openlibrary.org/b/isbn/9780345816023-L.jpg"
  },
  {
    id: 22,
    title: "Анна Каренина",
    author: "Лев Толстой",
    genre: "romance",
    language: "russian",
    price: 13.99,
    rating: 4.3,
    image: "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRciuDrg1jmFxBMFD_lf5qgCdorAljNaOiPJA&s"
  },
  {
    id: 23,
    title: "Ночной Дозор",
    author: "Сергей Лукьяненко",
    genre: "fantasy",
    language: "russian",
    price: 16.99,
    rating: 4.9,
    image: "https://i.redd.it/71cu77ycawcd1.jpeg"
  },
  {
    id: 24,
    title: "Ikki eshik orasi",
    author: "O'tkir Hoshimov",
    genre: "classic",
    language: "uzbek",
    price: 30.99,
    rating: 4.7,
    image: "https://kitobxon.com/img_knigi/1455.jpg"
  }
];

let LANG_LABEL = {
  english: 'EN',
  russian: 'RU',
  uzbek: 'UZ'
};

let NO_IMG_GRADIENTS = {
  english: 'linear-gradient(145deg, #0e1828, #0a1220)',
  russian: 'linear-gradient(145deg, #1a0e22, #110a18)',
  uzbek: 'linear-gradient(145deg, #0e1a0e, #091409)'
};

let state = {
  books: [],
  filters: {
    language: 'all',
    genre: 'all',
    price: 'all'
  },
  sort: 'default',
  search: ''
};

let searchTimer = null;


function getElement(id) {
  return document.getElementById(id);
}


function initializeBooks() {
  for (let i = 0; i < CATALOGUE.length; i++) {
    let book = CATALOGUE[i];
    let newBook = {
      id: book.id,
      title: book.title,
      author: book.author,
      genre: book.genre,
      language: book.language,
      price: book.price,
      rating: book.rating,
      image: book.image,
      liked: false,
      inCart: false
    };
    state.books.push(newBook);
  }
}


function makeStarHtml(rating) {
  let full = Math.floor(rating);
  let empty = 5 - full;

  let fullStars = '';
  for (let i = 0; i < full; i++) {
    fullStars = fullStars + '★';
  }

  let emptyStars = '';
  for (let i = 0; i < empty; i++) {
    emptyStars = emptyStars + '★';
  }

  let html = '<span class="stars-full">' + fullStars + '</span>';
  html = html + '<span class="stars-empty">' + emptyStars + '</span>';
  return html;
}


function getBookInitials(title) {
  let words = title.split(' ');
  let initials = '';

  for (let i = 0; i < 2 && i < words.length; i++) {
    let letter = words[i].charAt(0);
    initials = initials + letter;
  }

  return initials.toUpperCase();
}


function getThumbnailHtml(book) {
  if (book.image) {
    let html = '<img class="modal-thumb" src="' + book.image + '" alt="' + book.title + '">';
    return html;
  } else {
    let gradient = NO_IMG_GRADIENTS[book.language];
    let html = '<div class="modal-thumb" style="background:' + gradient + '"></div>';
    return html;
  }
}


function showToastMessage(icon, message) {
  let toastWrap = getElement('toastWrap');
  let element = document.createElement('div');
  element.className = 'toast';
  element.innerHTML = '<span class="toast-ico">' + icon + '</span><span>' + message + '</span>';
  toastWrap.appendChild(element);

  setTimeout(function() {
    element.classList.add('out');
    setTimeout(function() {
      element.remove();
    }, 320);
  }, 2600);
}


function updateBadgesDisplay() {
  let likeCount = 0;
  let cartCount = 0;

  for (let i = 0; i < state.books.length; i++) {
    if (state.books[i].liked) {
      likeCount = likeCount + 1;
    }
    if (state.books[i].inCart) {
      cartCount = cartCount + 1;
    }
  }

  let likeBadge = getElement('likeBadge');
  let cartBadge = getElement('cartBadge');
  let likeNavBtn = getElement('likeNavBtn');

  likeBadge.textContent = likeCount;
  if (likeCount > 0) {
    likeBadge.classList.add('show');
  } else {
    likeBadge.classList.remove('show');
  }

  cartBadge.textContent = cartCount;
  if (cartCount > 0) {
    cartBadge.classList.add('show');
  } else {
    cartBadge.classList.remove('show');
  }

  if (likeCount > 0) {
    likeNavBtn.classList.add('is-liked');
  } else {
    likeNavBtn.classList.remove('is-liked');
  }
}


function getFilteredBooks() {
  let result = [];

  for (let i = 0; i < state.books.length; i++) {
    let book = state.books[i];
    let shouldShow = true;

    if (state.filters.language !== 'all' && book.language !== state.filters.language) {
      shouldShow = false;
    }

    if (state.filters.genre !== 'all' && book.genre !== state.filters.genre) {
      shouldShow = false;
    }

    if (state.filters.price === 'cheaper' && book.price >= 25) {
      shouldShow = false;
    }

    if (state.filters.price === 'expensive' && book.price < 25) {
      shouldShow = false;
    }

    if (state.search !== '') {
      let searchTerm = state.search.toLowerCase();
      let titleMatches = book.title.toLowerCase().indexOf(searchTerm) !== -1;
      let authorMatches = book.author.toLowerCase().indexOf(searchTerm) !== -1;

      if (!titleMatches && !authorMatches) {
        shouldShow = false;
      }
    }

    if (shouldShow) {
      result.push(book);
    }
  }

  if (state.sort === 'az') {
    result.sort(function(a, b) {
      return a.title.localeCompare(b.title);
    });
  }

  if (state.sort === 'za') {
    result.sort(function(a, b) {
      return b.title.localeCompare(a.title);
    });
  }

  return result;
}


function getCardHtml(book, index) {
  let hasCover = book.image ? true : false;
  let fallback = NO_IMG_GRADIENTS[book.language] || NO_IMG_GRADIENTS.english;
  let initials = getBookInitials(book.title);
  let genreName = book.genre.charAt(0).toUpperCase() + book.genre.slice(1);
  let likeClass = book.liked ? 'liked' : '';
  let cartButtonText = book.inCart ? '✓ Added' : '+ Cart';
  let cartClass = book.inCart ? 'in-cart' : '';
  let fillColor = book.liked ? 'currentColor' : 'none';
  let langLabel = LANG_LABEL[book.language] || 'XX';
  let delay = (index % 24) * 0.038;

  let html = '<article class="book-card" data-id="' + book.id + '" style="animation-delay:' + delay + 's">';
  html = html + '<div class="book-card__cover ' + (hasCover ? '' : 'no-img') + '" data-initials="' + initials + '" style="background:' + fallback + '">';

  if (hasCover) {
    html = html + '<img class="book-card__img" src="' + book.image + '" alt="' + book.title + '" loading="lazy">';
  }

  html = html + '<div class="book-card__overlay"></div>';
  html = html + '<span class="book-card__lang">' + langLabel + '</span>';
  html = html + '<button class="book-card__like ' + likeClass + '" data-id="' + book.id + '">';
  html = html + '<svg width="14" height="14" viewBox="0 0 24 24" fill="' + fillColor + '" stroke="currentColor" stroke-width="2.2">';
  html = html + '<path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>';
  html = html + '</svg></button></div>';
  html = html + '<div class="book-card__body">';
  html = html + '<span class="book-card__genre genre--' + book.genre + '">' + genreName + '</span>';
  html = html + '<h3 class="book-card__title">' + book.title + '</h3>';
  html = html + '<p class="book-card__author">' + book.author + '</p>';
  html = html + '<div class="book-card__stars">' + makeStarHtml(book.rating) + '<span class="rating-val">' + book.rating + '</span></div>';
  html = html + '<div class="book-card__footer">';
  html = html + '<span class="book-card__price">$' + book.price.toFixed(2) + '</span>';
  html = html + '<button class="book-card__cart ' + cartClass + '" data-id="' + book.id + '">' + cartButtonText + '</button>';
  html = html + '</div></div></article>';

  return html;
}


function renderBooks() {
  let visible = getFilteredBooks();
  let bookCount = getElement('bookCount');
  let booksGrid = getElement('booksGrid');

  bookCount.textContent = visible.length;

  if (visible.length > 0) {
    let html = '';
    for (let i = 0; i < visible.length; i++) {
      html = html + getCardHtml(visible[i], i);
    }
    booksGrid.innerHTML = html;
  } else {
    let emptyHtml = '<div class="empty-state">';
    emptyHtml = emptyHtml + '<div class="empty-state__icon">📚</div>';
    emptyHtml = emptyHtml + '<p class="empty-state__text">No books found</p>';
    emptyHtml = emptyHtml + '<p class="empty-state__sub">Try a different search or adjust the filters</p>';
    emptyHtml = emptyHtml + '</div>';
    booksGrid.innerHTML = emptyHtml;
  }
}


function getModalRowsHtml(books, attrName) {
  let html = '';

  for (let i = 0; i < books.length; i++) {
    let book = books[i];
    html = html + '<div class="modal-row">';
    html = html + getThumbnailHtml(book);
    html = html + '<div class="modal-info">';
    html = html + '<div class="modal-book-title">' + book.title + '</div>';
    html = html + '<div class="modal-book-author">' + book.author + '</div>';
    html = html + '<div class="modal-book-price">$' + book.price.toFixed(2) + '</div>';
    html = html + '</div>';
    html = html + '<button class="modal-remove" data-' + attrName + '="' + book.id + '">✕</button>';
    html = html + '</div>';
  }

  return html;
}


function openModal(id) {
  let modal = getElement(id);
  modal.classList.add('open');
  document.body.style.overflow = 'hidden';
}


function closeModal(id) {
  let modal = getElement(id);
  modal.classList.remove('open');
  document.body.style.overflow = '';
}


function findBookById(id) {
  for (let i = 0; i < state.books.length; i++) {
    if (state.books[i].id === id) {
      return state.books[i];
    }
  }
  return null;
}


function attachCartClickHandler() {
  let booksGrid = getElement('booksGrid');

  booksGrid.addEventListener('click', function(e) {
    let likeButton = e.target.closest('.book-card__like');
    let cartButton = e.target.closest('.book-card__cart');

    if (likeButton) {
      let bookId = parseInt(likeButton.dataset.id);
      let book = findBookById(bookId);

      if (book) {
        book.liked = !book.liked;
        let message = book.liked ? 'Liked "' + book.title + '"' : 'Removed from liked';
        let icon = book.liked ? '♥' : '♡';
        showToastMessage(icon, message);
        updateBadgesDisplay();
        renderBooks();
      }
    }

    if (cartButton) {
      let bookId = parseInt(cartButton.dataset.id);
      let book = findBookById(bookId);

      if (book) {
        book.inCart = !book.inCart;
        let message = book.inCart ? '"' + book.title + '" added to cart' : 'Removed from cart';
        showToastMessage('🛒', message);
        updateBadgesDisplay();
        renderBooks();
      }
    }
  });
}


function attachFilterClickHandlers() {
  let filterGroups = document.querySelectorAll('.navbar__bottom [data-group]');

  for (let i = 0; i < filterGroups.length; i++) {
    let group = filterGroups[i];

    group.addEventListener('click', function(e) {
      let button = e.target.closest('.filter-btn');

      if (!button) {
        return;
      }

      let allButtons = this.querySelectorAll('.filter-btn');
      for (let j = 0; j < allButtons.length; j++) {
        allButtons[j].classList.remove('active');
      }

      button.classList.add('active');

      let groupName = this.dataset.group;
      let buttonValue = button.dataset.val;

      if (groupName === 'sort') {
        state.sort = buttonValue;
      } else {
        state.filters[groupName] = buttonValue;
      }

      renderBooks();
    });
  }
}


function attachSearchHandler() {
  let searchInput = getElement('searchInput');

  searchInput.addEventListener('input', function(e) {
    clearTimeout(searchTimer);

    searchTimer = setTimeout(function() {
      state.search = e.target.value.trim();
      renderBooks();
    }, 220);
  });
}


function attachModalCloseHandlers() {
  let closeButtons = document.querySelectorAll('.modal-close-btn');

  for (let i = 0; i < closeButtons.length; i++) {
    let button = closeButtons[i];
    button.addEventListener('click', function() {
      let modalId = this.dataset.modal;
      closeModal(modalId);
    });
  }

  let backdrops = document.querySelectorAll('.modal-backdrop');

  for (let i = 0; i < backdrops.length; i++) {
    let backdrop = backdrops[i];
    backdrop.addEventListener('click', function(e) {
      if (e.target === this) {
        closeModal(this.id);
      }
    });
  }
}


function attachLikeModalHandler() {
  let likeNavBtn = getElement('likeNavBtn');

  likeNavBtn.addEventListener('click', function() {
    let likedBooks = [];

    for (let i = 0; i < state.books.length; i++) {
      if (state.books[i].liked) {
        likedBooks.push(state.books[i]);
      }
    }

    let likeModalBody = getElement('likeModalBody');

    if (likedBooks.length > 0) {
      likeModalBody.innerHTML = getModalRowsHtml(likedBooks, 'unlike');
    } else {
      likeModalBody.innerHTML = '<div class="modal-empty">No liked books yet ♡</div>';
    }

    let removeButtons = likeModalBody.querySelectorAll('[data-unlike]');

    for (let i = 0; i < removeButtons.length; i++) {
      let button = removeButtons[i];
      button.addEventListener('click', function() {
        let bookId = parseInt(this.dataset.unlike);
        let book = findBookById(bookId);

        if (book) {
          book.liked = false;
          updateBadgesDisplay();
          renderBooks();
        }

        likeNavBtn.click();
      });
    }

    openModal('likeModal');
  });
}


function attachCartModalHandler() {
  let cartNavBtn = getElement('cartNavBtn');

  cartNavBtn.addEventListener('click', function() {
    let cartBooks = [];

    for (let i = 0; i < state.books.length; i++) {
      if (state.books[i].inCart) {
        cartBooks.push(state.books[i]);
      }
    }

    let cartModalBody = getElement('cartModalBody');
    let cartFoot = getElement('cartFoot');
    let cartTotal = getElement('cartTotal');

    if (cartBooks.length > 0) {
      cartModalBody.innerHTML = getModalRowsHtml(cartBooks, 'remove');
      cartFoot.style.display = 'flex';

      let total = 0;
      for (let i = 0; i < cartBooks.length; i++) {
        total = total + cartBooks[i].price;
      }
      cartTotal.textContent = '$' + total.toFixed(2);

      let removeButtons = cartModalBody.querySelectorAll('[data-remove]');

      for (let i = 0; i < removeButtons.length; i++) {
        let button = removeButtons[i];
        button.addEventListener('click', function() {
          let bookId = parseInt(this.dataset.remove);
          let book = findBookById(bookId);

          if (book) {
            book.inCart = false;
            updateBadgesDisplay();
            renderBooks();
          }

          cartNavBtn.click();
        });
      }
    } else {
      cartModalBody.innerHTML = '<div class="modal-empty">Your cart is empty 🛒</div>';
      cartFoot.style.display = 'none';
    }

    openModal('cartModal');
  });
}


function attachCheckoutHandler() {
  let checkoutButton = document.querySelector('.modal-checkout-btn');

  checkoutButton.addEventListener('click', function() {
    closeModal('cartModal');
    showToastMessage('✨', 'Thank you for your order!');

    for (let i = 0; i < state.books.length; i++) {
      state.books[i].inCart = false;
    }

    updateBadgesDisplay();
    renderBooks();
  });
}


function attachScrollButtonHandler() {
  let scrollBtn = getElement('scrollBtn');

  window.addEventListener('scroll', function() {
    if (window.scrollY > 500) {
      scrollBtn.classList.add('visible');
    } else {
      scrollBtn.classList.remove('visible');
    }
  });

  scrollBtn.addEventListener('click', function() {
    window.scrollTo({
      top: 0,
      behavior: 'smooth'
    });
  });
}


function initStarfield() {
  let canvas = getElement('starCanvas');
  let ctx = canvas.getContext('2d');
  let stars = [];
  let time = 0;

  function setupStars() {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;

    let starCount = Math.floor(canvas.width * canvas.height / 6000);

    for (let i = 0; i < starCount; i++) {
      let star = {
        x: Math.random() * canvas.width,
        y: Math.random() * canvas.height,
        r: Math.random() * 1.4 + 0.2,
        opacity: Math.random() * 0.7 + 0.15,
        speed: Math.random() * 0.004 + 0.001,
        phase: Math.random() * Math.PI * 2
      };
      stars.push(star);
    }
  }

  function drawStars() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    time = time + 0.012;

    for (let i = 0; i < stars.length; i++) {
      let star = stars[i];
      ctx.beginPath();
      ctx.arc(star.x, star.y, star.r, 0, Math.PI * 2);

      let opacity = star.opacity * (0.6 + 0.4 * Math.sin(time * star.speed * 300 + star.phase));
      ctx.fillStyle = 'rgba(220,210,190,' + opacity + ')';
      ctx.fill();
    }

    requestAnimationFrame(drawStars);
  }

  window.addEventListener('resize', function() {
    setupStars();
  });

  setupStars();
  drawStars();
}


function startApp() {
  initializeBooks();
  renderBooks();
  updateBadgesDisplay();

  attachCartClickHandler();
  attachFilterClickHandlers();
  attachSearchHandler();
  attachModalCloseHandlers();
  attachLikeModalHandler();
  attachCartModalHandler();
  attachCheckoutHandler();
  attachScrollButtonHandler();
  initStarfield();
}


startApp();